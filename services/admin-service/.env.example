SERVER_PORT=8089

# ── Neon PostgreSQL — admin_db (ep-floral-lab-anyb9djd, region: c-6 us-east-1) ──
# BUG FIX: was pointing to localhost:5432 (local DB)
DB_URL=jdbc:postgresql://ep-floral-lab-anyb9djd-pooler.c-6.us-east-1.aws.neon.tech/admin_db?sslmode=require&channelBinding=require
DB_USERNAME=neondb_owner
DB_PASSWORD=replace_with_neon_password

# ── JWT (shared secret — same value in all services) ──
JWT_SECRET_BASE64=replace_with_base64_secret
JWT_ISSUER=fast-link-identity
